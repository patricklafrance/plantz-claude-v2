# Fixes 3

## What I want

Apply the following UX and UI fixes across the application:

### 1. Fix cancel button contrast in view/edit modal

The cancel button in the view/edit modal does not have enough contrast to meet accessibility rules. Investigate the current styling and fix it so the button meets WCAG contrast requirements.

### 2. Add drag-and-drop reordering to both plant lists

Add a feature that allows users to re-order list items in both the Management and Today plant lists via drag-and-drop. The new order should persist for the duration of the session.

## Use the ADLC orchestrator

Build this task using the `plantz-adlc-orchestrator` skill.
